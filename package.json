{
  "name": "quill-cove",
  "version": "1.0.0",
  "scripts": {
    "start": "node Backend/server.js",
    "build": "cd QuillCove && npm install && npm run build",
    "serve": "serve -s QuillCove/dist",
    "postinstall": "npm install --prefix QuillCove && npm run build --prefix QuillCove"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "serve": "^11.3.0"
  }
}

